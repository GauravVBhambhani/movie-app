interface GhostboxProps {
    getPreview: (filename: string) => Promise<any>;
    hostDir: string;
    children: any;
}
export default function Ghostbox({ getPreview, hostDir, children }: GhostboxProps): JSX.Element;
export {};
